class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        n=len(prices)
        nostock = [0]*n
        hasstock = [0]*n
        hasstock[0]=-prices[0]
        for i in range(1,n):
            nostock[i]=max(hasstock[i-1]+prices[i],nostock[i-1])
            if i < 2:
                hasstock[i]=max(hasstock[i-1],-prices[i])
            else:
                hasstock[i]=max(hasstock[i-1],nostock[i-2]-prices[i])
            print(i,nostock[i],hasstock[i])
        return max(hasstock[n-1],nostock[n-1])
        
        
        
        https://blog.csdn.net/fuxuemingzhu/article/details/82656899
