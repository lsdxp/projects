class Solution:
    def getRow(self, rowIndex: int) -> List[int]:
        pre = [1]
        res = [1]
        for i in range(1,rowIndex+1):
            res = (i+1)*[1]
            for j in range(1,i):
                res[j]=pre[j-1]+pre[j]
            pre = res
        return res
