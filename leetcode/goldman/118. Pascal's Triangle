class Solution:
    def generate(self, numRows: int) -> List[List[int]]:
        res = [[1]]
        for i in range(1,numRows):
            tmp = (i+1)*[1]
            for j in range(1,i):
                tmp[j]=res[i-1][j-1]+res[i-1][j]
            res.append(tmp)
        return res
