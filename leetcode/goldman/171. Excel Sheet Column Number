class Solution:
    def titleToNumber(self, columnTitle: str) -> int:
        sum=0
        for idx,i in enumerate(columnTitle[::-1]):
            sum+=(ord(i)-64)*pow(26,idx)
        
        return sum
